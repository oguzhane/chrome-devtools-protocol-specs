{
    "openapi": "3.0.2",
    "info": {
        "title": "Chrome DevTools Protocol",
        "description": "Auto-generated OpenAPI 3.0 specification for Chrome Dev Tools Protocol's types, events, commands",
        "version": "1.0.0"
    },
    "paths": {},
    "components": {
        "schemas": {
            "EventDragInterceptedParams": {
                "description": "Emitted only when `Input.setInterceptDrags` is enabled. Use this data with `Input.dispatchDragEvent` to\nrestore normal drag and drop behavior.",
                "type": "object",
                "properties": {
                    "data": {
                        "$ref": "./Input.types.json#/components/schemas/TypeDragData"
                    }
                },
                "required": [
                    "data"
                ]
            }
        }
    }
}